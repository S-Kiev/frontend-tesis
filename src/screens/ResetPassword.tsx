import { FC } from 'react';

interface ResetPasswordProps {}

const ResetPassword: FC<ResetPasswordProps> = () => {
  return (
      <div>Content of Reset Password</div>
  );
};

export default ResetPassword;